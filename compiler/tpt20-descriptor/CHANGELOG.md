# Changelog

All notable changes to `tpt20-descriptor` are documented here.

## [Unreleased]

### Added
- Initial `Descriptor` type wrapping `tpt20_ir::PackageIr` (Phase 3, spec §8)
- JSON serialization / deserialization
- Binary serialization / deserialization with stable deterministic layout
- Dynamic lookup by name and id
- Reflection support hooks
