# Changelog

All notable changes to `tpt20-compat-protobuf` are documented here.

## [Unreleased]

### Added
- Initial protobuf compatibility adapter (Phase 14, spec §10)
- `.proto` schema import (proto2, proto3, Editions)
- Proto2 message, enum, oneof, map, service, option, reserved, and extension support
- Protobuf wire format decode adapter
- Protobuf wire format encode adapter
- `tpt20-cli` `import-proto` wiring
