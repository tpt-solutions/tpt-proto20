//! Embedded standard library `.tpt` schemas (spec §15).
//!
//! The canonical source of truth for code generation and descriptor exchange
//! is the `schema/stdlib.tpt` file alongside this crate. The corresponding
//! Rust types are defined in the parent crate.
