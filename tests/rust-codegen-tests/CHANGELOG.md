# Changelog

All notable changes to `tpt20-codegen-tests` are documented here.

## [Unreleased]

### Added
- Initial compile-and-run tests for generated Rust code
- Build script compiling fixture schema into `OUT_DIR`
- Tests for wire roundtrips, canonical encoding, unknown-field preservation, limits, views, JSON, and builders
